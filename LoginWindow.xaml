<Window x:Class="InspirationLabProjectStanSeyit.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="400" Width="500" Background="#bbeaba" WindowStartupLocation="CenterScreen">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Welcome Back!" FontSize="32" FontWeight="Bold" Foreground="#2e2e2e" 
                   HorizontalAlignment="Center" Margin="0,0,0,30"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <Label Content="Username" FontSize="14" FontWeight="Bold" Foreground="#2e2e2e"/>
            <TextBox Name="UsernameBox" Height="40" Padding="10" FontSize="11"
                     BorderBrush="#61be63" BorderThickness="2" Background="White"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,30">
            <Label Content="Password" FontSize="14" FontWeight="Bold" Foreground="#2e2e2e"/>
            <PasswordBox Name="PasswordBox" Height="40" Padding="10" FontSize="12"
                        BorderBrush="#61be63" BorderThickness="2" Background="White"/>
        </StackPanel>

        <Button Grid.Row="3" Content="Login" Width="200" Height="50"
                HorizontalAlignment="Center" 
                Background="#61be63" Foreground="White"
                FontSize="18" FontWeight="Bold"
                BorderThickness="0"
                Cursor="Hand"
                Click="Login_Click"/>
    </Grid>
</Window>
